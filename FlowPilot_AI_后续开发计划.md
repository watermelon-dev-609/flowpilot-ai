# FlowPilot AI · 后续开发计划

> 制定时间：2026-09-15
> 最近更新：2026-09-17
> 制定依据：`FlowPilot_AI_开发文档.md` §0 实际实现现状（实测核对）、`FlowPilot_AI_开发日志.md`、各阶段完成记录。
> 本文档是**后续开发的唯一执行依据**。历史规划（项目计划 §6.2、开发文档 §1–§15）不代表现状。

---

## 1. 起点盘点

### 1.1 已确认的实测基线（2026-09-17）

| 项目 | 实测值 |
|---|---|
| 后端测试 | 92 passed |
| 前端测试 | 43 files / 205 passed |
| 前端页面 | 13 个路由 |
| 后端 API | 35 个端点 |
| 数据持久化 | content_calendar 已接 SQLAlchemy Repository；rules / geo_monitor 已有 JSON / SQLAlchemy Repository 过渡实现；auth_users / auth_sessions 已接 SQLAlchemy；publish_queue / report_snapshots 仍为 JSON；前端 localStorage 仍保留 |
| 数据库 | SQLite 本地库兜底 + PostgreSQL 连接串预留；pgvector 未接入 |
| 鉴权 | S2.1 / S2.2 已完成：用户表、密码哈希、Bearer Token 登录 / 当前用户 / 退出；写操作权限校验待接入 |
| LLM / RAG / Agent | 无 |
| Git 分支 | `codex/p2-0-dynamic-rules-real-geo-monitor`，工作区干净 |

### 1.2 已完成的能力资产

规则中心（含来源复核、动态更新计划）、GEO 监测（五页 + 证据复核 + 周报导出）、
GEO 研究（选题图谱 + 评分）、内容适配、内容日历（含后端 API 与分页）、发布准备队列、引用准备度。

**判断**：功能广度已充分。当前瓶颈**不是功能不够，而是工程底座不牢**。

### 1.3 核心矛盾

当前项目在面试场景下有四个**容易被追问且答不上来**的点：

1. 「数据存哪？」→ 目前答：本地 JSON 文件。**面试官会立刻质疑这不算数据库能力。**
2. 「多人怎么用？」→ 目前答：已有登录与会话底座，但**写接口权限校验尚未全量接入**。
3. 「AI 在哪？」→ 目前答：没有真实调用。**项目名含 AI，但零 LLM 调用。**
4. 「响应式验证过吗？」→ 目前答：只有测试级，无截图证据。

**后续计划的主线目标：把这四点补掉。**

---

## 2. 总体路线

采用「**先补底座，再做 AI，最后收口叙事**」的顺序。

```text
S1 数据层正规化        ← 当前起点，最高优先级
   ↓
S2 鉴权与权限          ← 依赖 S1 的用户表
   ↓
S3 真实 AI 能力接入     ← 依赖 S1 的数据层，独立性较弱
   ↓
S4 工程收口            ← 响应式验收、文档、README、Case Study
```

**为什么这个顺序**：

- S1 是**一切的地基**。S2 需要用户表，S3 需要把 AI 结果落库，S4 需要稳定的系统才能截图。跳过 S1 做别的，后面都要返工。
- S1 直接补掉「数据库建模能力」这个简历最硬的短板。
- S3 放第三而非第二：AI 接入涉及 API Key、费用、外部依赖，不确定性最高，应在底座稳定后再做，避免被卡住。
- S4 放最后：需要前面都稳定后才有素材。

---

## 3. S1 数据层正规化（最高优先级）

**目标**：把本地 JSON 换成 PostgreSQL，补 Repository 抽象，让「数据库建模能力」在简历上立得住。

### 3.1 为什么必须先做

- 三个本地 JSON 加起来已 850KB（`rules.local.json` 576KB、`geo-monitor.local.json` 184KB、`content-calendar.local.json` 90KB），**单机文件读写已接近极限**。
- 本地 JSON 无事务、无并发控制、无索引，一旦数据量上涨或多人使用必然出问题。
- 这是简历「MySQL / PostgreSQL 数据库建模」这条能力的**唯一落点**，目前是空的。

### 3.2 任务拆解

| 编号 | 任务 | 产出 | 验收标准 | 状态 |
|---|---|---|---|---|
| S1.1 | 设计 PostgreSQL 数据模型 | 表结构文档 + ER 图 | 覆盖 rules / geo_sessions / geo_records / content_plans / audit_logs | ✅ 已完成（本次只做 content_calendar 部分） |
| S1.2 | 引入 SQLAlchemy + 数据层基础设施 | 依赖、配置、建表 | 可自动建全部表 | ✅ 已完成 |
| S1.3 | 实现 Repository 层 | 抽象契约 + JSON/SQL 双实现 + 工厂 | 业务逻辑不直接写 SQL，接口与现有 store 对齐 | ✅ 已完成 |
| S1.4 | Store → Repository 迁移 | content_calendar 改造 | 既有内容日历 API 测试不改断言即通过 | ✅ 已完成（当前后端全量 75 passed） |
| S1.5 | 补数据层测试 | 新增 19 个用例 | 覆盖正常 / 边界 / 异常；覆盖事务回滚与降级 | ✅ 已完成 |
| S1.6 | 数据迁移脚本 | 导入脚本 | 本地 JSON 可一次性导入数据库，导入后数据一致 | ✅ 已完成 |
| S1.7 | 前端数据源切换 | API 优先确认 | 前端无需改动逻辑，仅确认 API 模式可用 | ✅ 已完成 |
| S1.8 | 迁移 rules / geo_monitor | 复用 S1 模式 | 迁移后全量测试通过 | ✅ 主体完成（rules / geo_monitor JSON + SQLAlchemy 过渡仓储已完成） |

### 3.2.1 本次执行说明（2026-09-15）

**环境决策**：本机无 PostgreSQL、无可用 Docker CLI，经确认采用「先写代码用 SQLite 跑通测试，
PG 连接串留待环境就绪后零代码切换」。连接串通过 `FLOWPILOT_DATABASE_URL` 环境变量注入。

**数据策略**：保留 JSON 双读过渡——数据库不可用时自动降级回本地 JSON，
保证离线 Demo 能力不因数据库故障而中断。

**实际产出文件**：

```text
backend/app/data/__init__.py
backend/app/data/models.py                    ORM 模型（两张表）
backend/app/data/database.py                  连接、会话、建表
backend/app/data/content_plan_repository.py   仓储契约 + JSON 实现 + SQLAlchemy 实现
backend/app/data/repository_factory.py        实现选择与降级
backend/tests/test_s1_content_plan_repository.py  19 个数据层用例
FlowPilot_AI_数据模型设计_S1.md               设计文档
```

**关键成果**：既有 content_calendar API 测试**未修改任何断言**全部通过；
当前后端 75 passed，前端 205 passed，生产构建通过。

> **关键约束（S1.4）**：现有测试是**回归安全网**。迁移过程中**不允许修改既有测试**来迁就实现。
> 如果测试必须改，说明接口契约被破坏了，那是设计问题，要回头修设计。

### 3.3 表结构初稿

```text
rules                   规则主表（含 channel_type / review_status / effective_status / data_mode）
rule_audit_logs         规则审计日志（每次创建/编辑/状态变更追加）
geo_sessions            监测会话
geo_records             监测记录（含 evidence_level / review_status）
geo_record_attachments  证据附件
geo_record_reviews      复核记录
content_calendar_plans  内容计划
content_plan_audit_logs 内容计划审计日志
```

**设计要点**：

- 所有表带 `created_at` / `updated_at`，审计表只追加不更新。
- `data_mode`（mock / manual / real）字段必须在数据库层保留，不能因为换库就丢掉隔离能力。
- 外键约束要真实建立（本地 JSON 时代没有约束，换库是补上的好时机）。
- 考虑并发：`content_calendar_plans` 的状态流转需条件更新（`UPDATE ... WHERE status = 期望值`）防竞态。

### 3.4 风险与注意

- **迁移期间数据双写风险**：建议一次性切换，不做长期双写，避免状态不一致。
- **测试数据库隔离**：pytest 需用独立测试库或事务回滚，不能污染开发数据。
- **Alembic 迁移不可逆**：每次迁移前先备份，迁移脚本不要写不可回滚的破坏性操作。

---

## 4. S2 鉴权与权限

**目标**：补上用户体系与真实后端权限校验，让「多人协作」「权限控制」不再是无解题。

### 4.1 为什么做

- 此前**后端零鉴权**；当前已补用户表、密码哈希、Bearer Token 登录 / 退出与当前用户接口，下一步接入写操作权限校验。
- 用户自己的编码规范明确写了「前端仅控制权限展示，绝不能作为安全边界」——**当前后端没有任何安全边界**。
- 任何公开部署都等于数据裸奔。

### 4.2 任务拆解

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S2.1 | 用户表与密码存储设计 | 密码哈希（PBKDF2-SHA256 + 随机盐），**绝不存明文**，日志脱敏 |
| S2.2 | 登录 / 登出 / 会话 | 使用 Bearer Token，便于 API 与前后端分离调用 |
| S2.3 | 后端权限中间件 | 每个写操作端点都有真实权限校验，非仅前端隐藏 |
| S2.4 | 基于角色的权限模型 | 至少区分「管理员 / 运营」两级，明确各角色可操作范围 |
| S2.5 | 前端无权限状态 | 前端补齐「无权限」第四态（当前只有 loading / empty / error） |
| S2.6 | 鉴权测试 | 覆盖未登录、越权、token 过期三类场景 |

### 4.3 风险与注意

- **不要自研加密**：用成熟库，不要手写哈希算法。
- **Token 存储位置**：避免 localStorage（XSS 可读），优先 httpOnly Cookie。
- **CORS 与 CSRF**：接入 Cookie 后必须同步处理 CSRF，否则引入新漏洞。
- **密钥管理**：JWT secret 等必须走环境变量，**禁止硬编码**。

---

## 5. S3 真实 AI 能力接入

**目标**：让项目名副其实——从「AI 主题的管理系统」变成「真正调用 AI 的系统」。

### 5.1 为什么做

- 项目名含 AI，但**零 LLM 调用**。这是面试时最难解释的一点。
- `开发文档 §5.4` 规划的 LLM Provider Adapter 是可落地且高价值的。
- 接入后，「Prompt Engineering」「大模型应用」两条简历能力才有实际支撑。

### 5.2 任务拆解

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S3.1 | LLM Provider Adapter 抽象 | 接口先行，支持多 Provider 可替换 |
| S3.2 | Mock Adapter 实现 | 无 API Key 也能跑通全流程，测试不依赖外部服务 |
| S3.3 | 真实 Provider 实现 | 至少接一个真实模型（DeepSeek / 通义 / 其他） |
| S3.4 | Prompt Library | Prompt 版本化管理，可追溯每次生成用了哪个版本 |
| S3.5 | 内容生成落地 | 把「内容适配」从结构展示升级为真实生成 |
| S3.6 | 生成结果落库 | 生成记录、耗时、Token 消耗入库，为简历量化数据做准备 |
| S3.7 | 失败与降级处理 | 超时、限流、Key 失效都要有明确错误态，不伪造成功 |

### 5.3 风险与注意

- **成本控制**：必须设置调用上限与超时，避免测试或误操作烧掉额度。
- **密钥安全**：API Key 走环境变量，**不进代码、不进 Git**。
- **测试策略**：单测一律用 Mock Adapter，真实调用只放在手动验收环节。
- **不夸大的表述**：接入 LLM 后仍需遵守 §0.5 边界，不承诺排名与引用。

---

## 6. S4 工程收口

**目标**：把已有成果转成可展示、可讲述、经得起追问的交付物。

### 6.1 任务拆解

| 编号 | 任务 | 验收标准 |
|---|---|---|
| S4.1 | 四断点响应式截图验收 | 375 / 768 / 1024 / 1440 全部页面截图留存 |
| S4.2 | 对抗性审查问题收口 | 核对 `对抗性代码审查.md` 中 S / M / H 问题的修复状态 |
| S4.3 | README 更新 | 补齐架构图、截图、技术栈现状、Roadmap |
| S4.4 | Case Study 整理 | 业务问题 → 设计 → 架构 → 截图 → 真实数据 → 问题与改进 |
| S4.5 | 简历表述校准 | 严格按 §0.5 边界书写，不写未实现的能力 |
| S4.6 | 量化数据整理 | 从 S3.6 落库数据中提取真实数字（调用次数、耗时、Token） |

### 6.2 四断点验收清单（S4.1 展开）

每个页面检查：

- [ ] 无横向滚动
- [ ] 关键表格在移动端转为卡片
- [ ] 导航在窄屏可用（抽屉或折叠）
- [ ] 文字无溢出、无截断错位
- [ ] 按钮点击区域足够（移动端 ≥ 44px）
- [ ] loading / empty / error / 无权限 四态均可见

---

## 7. 需要你决策的问题

以下是**必须由你确认**才能继续的决策点，不确认会导致返工：

### 7.1 产品中心这条线要不要补做？

原始定位是「方案 A：**产品内容**与 GEO 闭环」，但实际走成了「**GEO 运营与监测**」路线。
产品中心、AI 产品理解、Product Card、Fact Check、GEO Critic、文章生成**全部未实现**。

- **选项 A：补做** → 回归原始定位，补产品中心与内容生成链路。工作量大，但项目叙事更完整。
- **选项 B：不补，正式修订定位** → 承认项目定位已演进为「GEO 运营工作台」，同步修改所有文档。成本低，叙事清晰。
- **选项 C：只补 Fact Check / GEO Critic** → 折中，只补内容质量把控这一段（可与 S3 合并）。

**我的建议：选 B 或 C。** 理由：当前「GEO 运营与监测」路线已经自成体系且有真实价值，
强行补产品中心会稀释主线；而 Fact Check / Critic 与 S3 的 AI 能力天然契合，性价比高。

### 7.2 S1 用 PostgreSQL 还是先从 SQLite 过渡？

- **直接 PostgreSQL**：一步到位，简历表述最硬。需要本地装 PostgreSQL。
- **先 SQLite 再迁移**：无需装环境，起步快。但简历上 SQLite 说服力弱，且要迁移两次。

**我的建议：直接 PostgreSQL。** 反正要写 Repository 抽象层，换库成本主要在配置不在代码。

### 7.3 S1 的规模要多大？

- **最小版**：只迁移 `content_calendar`（数据量最小、测试最全），验证方案可行性。
- **完整版**：三个 store 一次性全迁。

**我的建议：先做最小版（S1 只迁 content_calendar），跑通后再迁其余两个。** 降低一次性风险。

---

## 8. 执行纪律（沿用现有工程规范）

每个小阶段必须遵守：

1. **测试先行**：先写失败测试（RED），再实现（GREEN）。
2. **不允许为迁就实现而改测试**。
3. **全量回归**：前端全量测试 + 后端全量测试 + 前端生产构建，三者都要过。
4. **安全扫描**：npm audit、pip-audit、Semgrep 必须通过。
5. **真实运行验证**：实际启动前后端，走一遍真实操作，不只看测试绿。
6. **记录**：在 `FlowPilot_AI_开发日志.md` 追加带日期的完成记录，含验证结果与风险点。
7. **收尾风险清单**：每次交付单独列出潜在风险与待确认项。

---

## 9. 里程碑

| 里程碑 | 内容 | 完成标志 |
|---|---|---|
| M1 | S1 数据层正规化 | content_calendar Repository 已完成；后续 rules / geo_monitor 迁移后全量测试不改即通过 |
| M2 | S2 鉴权与权限 | 未登录 / 越权 / 过期三类测试通过 |
| M3 | S3 真实 AI 接入 | Mock 与真实 Provider 均可用，生成结果落库 |
| M4 | S4 工程收口 | 四断点截图留存，README + Case Study 完成 |

**建议节奏**：M1 是所有后续工作的前提，应最优先完成。M2 与 M3 可并行（S3 不强依赖 S2）。

---

## 10. 当前待办（立即可执行）

按优先级排序，**建议从第 1 项开始**：

1. **S2 第二段**：将后端写操作端点接入权限校验，覆盖未登录 / 越权测试。
2. **S1.8 后续增强**：将 rules / geo_monitor 的审计、来源复核、证据附件拆成独立明细表。

> 注：S1 开始前建议先备份三个本地 JSON 数据文件。
