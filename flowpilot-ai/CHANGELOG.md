# 更新日志

本文档记录 FlowPilot AI 的重要版本变化。

## 2026-09-17

### 新增

- 首页台账参数已落到业务目标页：
  - `/publish-queue`
  - `/geo-monitor/records`
  - `/geo-monitor/report`
- 新增 `content_calendar` 数据层 Repository：
  - SQLAlchemy ORM 模型
  - 数据库连接与会话管理
  - JSON / SQLAlchemy 双实现
  - 数据库不可用时降级 JSON
  - 覆盖 CRUD、筛选排序分页、事务回滚、级联删除、降级和契约一致性的后端测试
- 新增 `content_calendar` JSON → 数据库迁移脚本：
  - 保留旧记录 `id`、时间字段和审计日志。
  - 重复执行按 `id` 跳过，避免重复导入。
  - 本地真实 JSON 验证：第一次导入 104 条，第二次跳过 104 条。
- 新增 SQLite Repository 模式 API 联通测试，覆盖内容计划创建、更新、重启后查询。
- 新增规则中心 Repository 抽象与 JSON 实现，RuleStore 改为可注入仓储。
- 新增规则中心 SQLAlchemy Repository 过渡实现，包含 `rules` 主表、常用索引字段与 `payload_json` 完整载荷持久化。

### 改进

- `ContentCalendarStore` 改为业务层，持久化委派给 Repository，同时保留既有 API 契约。
- 后端依赖新增 `SQLAlchemy>=2.0`。

### 验证

- 前端完整测试通过：43 个测试文件，205 条用例通过。
- 前端生产构建通过。
- 后端完整测试通过：84 条用例通过。

## v1.1.0 - 2026-09-14

### 新增

- 新增运营工作台首页，替代早期项目说明型首页。
- 新增全局导航，统一连接规则中心、GEO 研究、内容适配、发布队列、GEO 监测、引用准备度和内容日历。
- 新增规则中心本地持久化、平台选择、来源复核和动态规则更新计划。
- 新增真实 GEO 监测最小闭环，区分品牌提及、页面检索和来源引用。
- 新增 GEO 监测数据概览、监测记录页、复核页、会话页和 Markdown 周报导出。
- 新增 GEO Research 与规则中心、监测数据联动。
- 新增选题优先级、难度和平台适配评分。
- 新增内容适配页面，支持母稿、多平台版本、平台策略、引用准备提示和发布建议。
- 新增发布队列页面，支持发布准备、失败原因和状态展示。
- 新增引用准备度页面，用于检查内容被 AI 检索和引用前的基础条件。
- 新增内容日历独立页面，支持关键词、状态、平台、负责人、优先级和日期范围筛选。

### 改进

- 页面可见文案统一中文化，避免把简历映射信息直接放入产品界面。
- 增加统一 API 响应契约、异步数据状态模型和数据状态展示组件。
- 将选题池本地仓储抽离为独立层，减少页面与浏览器存储的耦合。
- 将内容日历从内容适配页面中拆出，降低单页复杂度。
- 增加项目计划记录，持续沉淀开发过程、验证结果、问题和风险。

### 验证

- 前端完整测试通过：33 个测试文件，115 条用例通过。
- 前端生产构建通过。
- 本地页面访问检查通过：
  - `/content-calendar` 返回 HTTP 200。
  - `/content-adaptation` 返回 HTTP 200。
  - `/geo-research` 返回 HTTP 200。

### 已知限制

- 当前主要使用浏览器本地存储，尚未切换为正式后端数据库。
- 筛选条件尚未写入 URL，刷新页面后不会保留。
- 规则更新和 GEO 监测仍是最小闭环，真实平台数据采集需要后续接入 API 或半自动复核流程。
- 发布队列仍偏发布准备与状态管理，尚未接入真实平台发布适配器。
- 不承诺 GEO 排名，也不承诺一定被任何 AI 平台引用。

## 文档维护说明 - 2026-09-15

本次未变更代码，仅校正文档失真。

### 校正

- `FlowPilot_AI_开发文档.md` 原文描述的是立项规划架构（PostgreSQL + pgvector + RAG + Agent + Product Center），
  与已实现系统差异较大。新增 §0「实际实现现状」与 §0.4「规划 vs 实现差异对照」，
  并将 §1–§15 标记为历史规划。
- `FlowPilot_AI_项目计划.md` §6 阶段表原文 P0–P10 全部标记「未开始」，
  与当时实际进度（后端 43 / 前端 147 测试通过）矛盾，已改为「实际完成路线 + 初始设想对照」。
- 新增 `FlowPilot_AI_后续开发计划.md`，定义 S1–S4 四阶段后续路线。

### 当时实测基线

- 后端测试：43 passed（2026-09-15 当时基线；2026-09-17 已提升到 75 passed）。
- 前端测试：34 files / 147 passed（2026-09-15 当时基线；2026-09-17 已提升到 43 files / 205 passed）。
- 前端页面：12 个路由（2026-09-17 已提升到 13 个）。
- 后端 API：25 个端点（2026-09-17 已提升到 32 个）。

### 尚未实现（重要）

- 数据库：2026-09-15 当时为本地 JSON + localStorage；2026-09-17 `content_calendar` 已接 SQLAlchemy Repository，`rules` 已有 SQLAlchemy 过渡仓储，PostgreSQL 连接串预留但未实机验证。
- 鉴权：后端无任何鉴权与权限校验。
- AI 能力：无 LLM / RAG / Agent 实现，无任何真实模型调用。
- 产品中心、Product Card、Fact Check、GEO Critic、文章生成：均未实现。
